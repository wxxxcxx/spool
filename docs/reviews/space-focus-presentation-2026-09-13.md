# 每 Space 焦点展示交付审查

## 范围

规格：[问题22](../wayfinding/declarative-state/issues/22-space-focus-presentation.md)。审查基准固定为基础提交`d79d264`，比较该提交与实现后的工作树（`git diff d79d264`），生产改动位于bar/layout.rs、bar/state.rs、ecs/focus.rs、ecs/systems.rs。此前焦点激活、初始列宽与Space边框绑定修复作为独立基础提交保留。

遵循用户不使用子代理的指示，由同一执行者分别核对规范与规格，不宣称独立专家审查。

## Standards

未发现阻塞项。焦点模块拥有局部选择与生命周期回退，Bar和边框只读取同一投影；复用WindowManager与原生装饰归属保护，无新增直接FFI。变更仅在局部选择实际改变时标记资源变化，确认焦点标记与历史未转作界面状态。严格Clippy、格式与编译通过。

## Spec

未发现缺失的代码路径。所有Space选择同样高亮，活动标记保持独立；折叠预览把选择提升到展示容量内并置于最前，不改变模型顺序。关闭/最小化使用同Space历史回退，无候选则清空；AX失联保留选择。边框不再因Space可见但非活动而移除目标，仍受可信几何与原生归属限制。

1230项workspace测试通过、0失败、2项忽略。新增回归先在旧Bar、折叠布局和边框目标路径观察到失败，再验证实现；生命周期回归覆盖关闭和最小化。

## 验证边界

代码与mock验证完成，debug二进制已构建。未重启用户的运行进程，未做原生桌面视觉复验；此项仍为交付限制，不能把测试通过写成现场效果已确认。
